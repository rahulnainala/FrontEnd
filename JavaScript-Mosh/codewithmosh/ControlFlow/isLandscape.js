function isLandscape(width, height) {
  //   return width > height ? true : false;
  //No need to write like above its poor way to write code
  return width > height;
}

console.log(isLandscape(3000, 2000));
